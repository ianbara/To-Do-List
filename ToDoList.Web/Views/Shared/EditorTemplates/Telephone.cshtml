@using Pfg.Websites.ProvidentOnline.Common
@model string

@if (System.Web.HttpContext.Current.Request.Browser["IsMobile"] == "True")
{
    @Html.TextBoxFor(m => m, new { type = "tel" })
}
else
{
    @Html.TextBoxFor(m => m);
}
